{
  "name": "portfolio-ethan",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "seed": "node scripts/seed.js",
    "deploy": "firebase deploy --only hosting,firestore:rules"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0"
  },
  "devDependencies": {
    "firebase-tools": "^13.0.0"
  }
}
